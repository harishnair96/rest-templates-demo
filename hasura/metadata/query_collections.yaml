- name: allowed-queries
  definition:
    queries:
      - name: Employees
        query: |-
          query Employees {
            Employee {
              Address
            }
          }
      - name: GetCustomers
        query: |-
          query GetCustomers($offset: Int = 0) {
            Customer(limit: 10, offset: $offset) {
              CustomerId
              FirstName
              LastName
              Email
            }
          }
